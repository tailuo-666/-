**Memory**

  1.和聊天 Agent 熟悉半个月后，用户问了一个以前问过的问题。Agent 如何做 memory 召回更合理？

​         采用多层次、动态化的策略：首先通过混合匹配（语义向量+关键词）识别重复问题；其次应用时间衰减函数调整记忆权重；然后整合多版本记忆内容；最后构建增强的上下文。
	  首先要区分：

- **情景记忆（Episodic）**：关于**具体事件**——时间、地点、参与者、发生了什么。例如「昨天下午用户让我把报表改成 PDF」。
- **语义记忆（Semantic）**：**一般性知识**——概念、事实、规则。例如「PDF 是便携式文档格式」「公司退货政策是 7 天」。


- **情景**常存为「带时间戳的对话片段、任务轨迹、工具调用日志」。
- **语义**常存为「稳定事实、规则、偏好、知识库条目、政策文档、FAQ、图谱三元组」。其中要明确 `scope`：用户级语义记忆（user）、租户/企业知识（tenant）、公共知识（global）不能混在一起。


- 情景检索偏**时间邻近 + 事件相似**；
- 语义检索偏**概念相似 + 权威来源**。
- **合并**：回答用户时先定位情景（我做过什么），再引用语义（规则是什么）。

​        接着，调用完可以对记忆做一个衰减权重。完成闭环。

​	而且，对于市面上的很多产品，有一个“引用”功能，这个也可以用于召回记忆。

​	如果不引用，那么可以用以上的方案。

​	另外，记忆的匹配也应注意：

- **基于时间**：最近对话优先，强时效任务（排障、联调）更有效。
- **基于相关性**：语义相似，embedding 近似的记忆优先，适合开放域问答、用户换说法。
- **基于重要性**：关键信息优先（项目目标、硬约束、用户级别偏好），适合长会话中早期出现但「仍然必须遵守」的内容。

  2.你理解的 Agent memory 经典框架是什么？它的发展趋势是什么，最头部的玩家在怎么做？

Agent Memory的经典框架已从单层存储模式演进到分层存储、知识图谱增强、分布式共享记忆等多种模式。分层存储模式（结构化状态+向量搜索）是当前最广泛采用的基础架构，而 记忆作为工具 是当前最显著的发展趋势，使Agent能够自主管理上下文，提高交互效率，比如“睡眠”功能。

设计上应该按照实际需求分类，以下是基本的分类：

1. 按时间尺度：短期（会话内）vs 长期（跨会话）。
2. 按内容类型：程序性（怎么做）、陈述性（事实）、情景性（何时何地何人）、语义性（抽象知识）
3. 按可控性：显式记忆（用户确认保存）vs 隐式记忆（系统自动提炼）。

更强调**分层 + 可学习检索 + 用户可控**；合规与可删除性成为默认需求；与**世界模型/仿真**结合用于更强规划。